# Deployment Guide for Base Developer Kit

## 1. Deploy on Base Sepolia (Testnet)

```bash
# Install dependencies
npm install

# Deploy Counter contract
npx hardhat run scripts/deploy.js --network baseSepolia
