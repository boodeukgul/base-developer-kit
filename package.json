{
  "name": "base-developer-kit",
  "version": "1.0.0",
  "description": "Complete developer kit for building on Base",
  "scripts": {
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network baseSepolia"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
